<?xml version="1.0" encoding="utf-8"?>
<!--
    Customer records and ID document photos must never leave the device, so
    both cloud backup and device-to-device transfer are excluded. This pairs
    with android:allowBackup="false" for older Android versions.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
